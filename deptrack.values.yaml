ingress:
  enabled: true
  tls:
    enabled: false
    secretName: ""
  annotations: {}
  host: dependencytrack.example.org
frontend:
  replicaCount: 1
  service:
    type: NodePort
apiserver:
  resources:
    requests:
      cpu: 1
      memory: 3000Mi
    limits:
      cpu: 2
      memory: 7Gi

# FORZAMOS LA CONFIGURACIÓN DE POSTGRESQL AQUÍ
postgresql:
  image:
    tag: 11.10.0
